{
  "name": "locate-the-place",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "mkdir -p assets/leaflet && cp -r node_modules/leaflet/dist/* assets/leaflet && watchify index.js -t brfs -o bundle.js | http-server -S",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "brfs": "^1.4.3",
    "haversine-distance": "^1.1.3",
    "howler": "^2.0.4",
    "leaflet": "^1.2.0",
    "regl": "^1.3.0"
  },
  "devDependencies": {
    "browserify": "^14.4.0",
    "watchify": "^3.9.0"
  }
}
